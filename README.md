# sqrib-ravenDB

Root workspace for the sqrib RavenDB project.

## Structure

- `docker-compose.yml` - Docker Compose setup for local services.
- `web/` - Web application source.

## Getting Started

From the repository root:

```powershell
docker compose up
```

Then follow the app-specific setup instructions inside `web/` if additional dependencies or commands are required.

